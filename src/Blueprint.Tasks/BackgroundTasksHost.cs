using Microsoft.Extensions.DependencyInjection;

namespace Blueprint.Tasks
{
    /// <summary>
    /// A host for executing background tasks (see <see cref="TasksConfigurationExtensions.BackgroundTasks" />).
    /// </summary>
    public class BackgroundTasksHost
    {
    }
}
