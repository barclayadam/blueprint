namespace Blueprint.Configuration
{
    /// <summary>
    /// A "host" of the Blueprint API, identifies in what environment the API is to be run to enable configuring things
    /// like operation scanner semantics. One, and only one, host <b>must</b> be specified.
    /// </summary>
    public interface IBlueprintApiHost
    {
    }
}
