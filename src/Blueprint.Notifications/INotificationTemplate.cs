namespace Blueprint.Notifications
{
    /// <summary>
    /// A template that represents a single communication protocol's (e.g. SMS, SMTP)
    /// template, an object that belongs to a single Notification and is used
    /// to send communication when a notification is raised.
    /// </summary>
    public interface INotificationTemplate
    {
    }
}